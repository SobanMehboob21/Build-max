apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: production
spec:
  selector:
    app: build-max-backend 
  ports:
    - port: 80
      targetPort: 5000
  type: LoadBalancer 